main:
    add $a0, $zero, $zero
    addi $a3,$zero,7
    addi $a1,$zero,13
loop:   
    sw $a0, 0($zero)
    jalr $a3,$s0
    addi $a0,$a0,1
    
    beq $a0,$a1,done
    beq $zero,$zero,loop
done: 
    nand $a2,$zero,$a1
    lw $k0,0($zero)
    
